# TODO: Fix mailtrapClient ReferenceError - FIXED CODE CHANGES ✅\n\n## Plan Steps\n\n- [x] Step 1: Edit backend/mailtrap/mailtrap.js - Rename export `mailtrapclient` → `mailtrapClient`\n- [x] Step 2: Edit backend/mailtrap/sendVerificationEmail.js - Update import `{ mailtrapclient` → `{ mailtrapClient`\n- [ ] Step 3: Restart server & test signup\n- [ ] Step 4: Check Mailtrap dashboard & complete\n\n**Next Action Required:**\n1. Create/add to `backend/.env`:\n```\nMAIL_TRAP_TOKEN=your_actual_mailtrap_token\nMAIL_TRAP_ENDPOINT=https://send.api.mailtrap.io/\n```\n   Get token from https://mailtrap.io → Integrations → Send Email API\n2. **Restart backend server** (Ctrl+C → `npm run dev` or `node backend/server.js`)\n3. Test signup endpoint\n4. Check Mailtrap Inbox for verification email
